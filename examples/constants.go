package examples

const (
	// LicenseKeyFilePath is the absolute path to the json file
	// downloaded from google console that containing project secret key
	LicenseKeyFilePath = `INSERT_ABSOLUTE_PATH_TO_THE_JSON_KEY`

	// ProjectID is the Google Project ID
	ProjectID = `GOOGLE_PROJECT_ID`
)
